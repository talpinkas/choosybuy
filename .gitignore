# Personal Claude Code settings (machine-local permission grants)
.claude/settings.local.json

# Browser-automation scratch (Playwright MCP)
.playwright-mcp/

# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db

# Push credentials (PAT) — local only, never commit
.secrets/

# Pilot participant data (names) — never commit to the public repo
private/
